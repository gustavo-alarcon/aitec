export interface GeneralConfig{
  categories: object[];
  salesRCounter: number;
  buysCounter: number;
  maxWeight: number;
  lastVersion: string;
  news: {
    visible: boolean,
    imageURL: string
  }
}